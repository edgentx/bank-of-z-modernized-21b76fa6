# GENERATED by deploy/argocd/generate.sh from deploy/helm/teller/values-microk8s.yaml.
# DO NOT EDIT BY HAND — re-run `deploy/argocd/generate.sh` after chart changes.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: teller-staging
  namespace: argocd
  labels:
    app.kubernetes.io/name: teller
    app.kubernetes.io/part-of: bank-of-z
    bank.example.com/environment: staging
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    notifications.argoproj.io/subscribe.on-sync-failed.slack: bank-deploys
    notifications.argoproj.io/subscribe.on-health-degraded.slack: bank-deploys
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: bank-of-z
  source:
    repoURL: https://github.com/edgentx/bank-of-z-modernized-21b76fa6
    targetRevision: main
    path: deploy/helm/teller
    helm:
      releaseName: teller
      valueFiles:
        - values.yaml
        - values-microk8s.yaml
  destination:
    name: microk8s-staging
    namespace: bank-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
  revisionHistoryLimit: 20
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
