NEXT_PUBLIC_APP_NAME=Bank-of-Z Teller (staging)
# Same-origin via in-pod nginx proxy (see .env.production for rationale).
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_ENVIRONMENT=staging
API_REQUEST_TIMEOUT_MS=15000
