export default function CustomersPage() {
  return (
    <section>
      <h2 className="text-2xl font-semibold text-teller">Customers</h2>
      <p className="mt-2 text-slate-600">
        Customer-management views (UpdateCustomerDetailsCmd / DeleteCustomerCmd surface) attach
        here once handler stories ship.
      </p>
    </section>
  );
}
