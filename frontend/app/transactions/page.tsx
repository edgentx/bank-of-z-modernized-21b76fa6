export default function TransactionsPage() {
  return (
    <section>
      <h2 className="text-2xl font-semibold text-teller">Transactions</h2>
      <p className="mt-2 text-slate-600">
        Deposit / withdrawal / transfer flows attach here as command-handler stories land.
      </p>
    </section>
  );
}
