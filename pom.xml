<!-- Partial snippet of changes to pom.xml to fix build -->
<project>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/>
    </parent>
    <!-- ... existing GAV ... -->
    
    <properties>
        <java.version>17</java.version>
        <!-- EXPLICITLY PIN Temporal Version as it is NOT in Spring Boot BOM -->
        <temporal.version>1.22.1</temporal.version>
    </properties>

    <dependencies>
        <!-- Fix for BUILD FAILED: Ensure Temporal dependency has explicit version -->
        <dependency>
            <groupId>io.temporal</groupId>
            <artifactId>temporal-spring-boot-starter</artifactId>
            <version>${temporal.version}</version>
        </dependency>
        
        <!-- Required for the new VW-454 E2E Regression Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
<!-- END SNIPPET -->