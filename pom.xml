<!-- Relevant changes only -->
<project>
    <dependencies>
        <!-- 
             FIX: org.flywaydb:flyway-db2 is NOT in the Spring Boot BOM. 
             We must explicitly define the version to prevent DependencyResolutionException.
        -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-db2</artifactId>
            <version>9.22.3</version>
        </dependency>
        <!-- Temporal SDK for workflow testing -->
        <dependency>
            <groupId>io.temporal</groupId>
            <artifactId>temporal-testing</artifactId>
            <version>1.20.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
