<!-- Root POM update for Dependency Management -->
<project>
    <!-- Existing POM content omitted for brevity -->
    
    <dependencies>
        <!-- Existing dependencies -->

        <!-- Temporal SDK for Java (Not in standard BOM) -->
        <dependency>
            <groupId>io.temporal</groupId>
            <artifactId>temporal-sdk</artifactId>
            <version>1.22.0</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Spring Boot Test (Standard BOM) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Mockito (Standard BOM) -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Awaitility (Standard BOM) -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>