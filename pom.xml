<!-- 
  FIX DEPENDENCY RESOLUTION ERROR
  The build failed because 'com.ibm.mq:mq-jms-spring-boot-starter:jar:3.0.4' and 
  'io.temporal:temporal-spring-boot-starter:jar:1.22.0' could not be resolved.

  According to Rule 9, if a dependency is not covered by the Spring Boot BOM, 
  an explicit <version> must be declared.

  The groupId 'com.ibm.mq' is NOT in the BOM closed set. Version added.
  The groupId 'io.temporal' is NOT in the BOM closed set. Version added.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.example</groupId>
    <artifactId>bank-of-z-modernization</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version> 
        <relativePath/>
    </parent>
    
    <properties>
        <java.version>17</java.version>
        <temporal.version>1.22.0</temporal.version>
        <mq.version>3.0.4</mq.version>
    </properties>

    <dependencies>
        <!-- Spring Stack -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Temporal (Explicit Version Required - Rule 9) -->
        <dependency>
            <groupId>io.temporal</groupId>
            <artifactId>temporal-spring-boot-starter</artifactId>
            <version>${temporal.version}</version>
        </dependency>

        <!-- IBM MQ (Explicit Version Required - Rule 9) -->
        <dependency>
            <groupId>com.ibm.mq</groupId>
            <artifactId>mq-jms-spring-boot-starter</artifactId>
            <version>${mq.version}</version>
        </dependency>
    </dependencies>
</project>