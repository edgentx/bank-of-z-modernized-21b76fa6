<project>
  <!-- Existing pom.xml structure -->
  <dependencies>
    <!-- Add Temporal and Slack testing dependencies if not present -->
    <dependency>
      <groupId>io.temporal</groupId>
      <artifactId>temporal-testing</artifactId>
      <version>1.22.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Spring Boot Test is implied by spring-boot-starter-test -->
  </dependencies>
</project>