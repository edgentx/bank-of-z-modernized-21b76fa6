package com.example.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for validation components.
 * Explicitly enabling component scanning for Adapters if not auto-detected,
 * though @ComponentScan in Application.java handles this.
 * This serves as a placeholder for future temporal workflow registration.
 */
@Configuration
public class ValidationConfig {
    // Future: Register Temporal Workflows and Activities here
}
