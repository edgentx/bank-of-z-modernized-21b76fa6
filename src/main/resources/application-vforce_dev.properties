# BANK S-FB-2 - VForce360 dev profile.
#
# vforce_dev does not provision the legacy DB2 history database. Keep the
# history repositories and Flyway migrations enabled, but run them against the
# same in-memory H2 DB2-compatibility setup used by the integration suite so
# the pod never falls back to a loopback DB2 listener at startup.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:mem:bank-vforce-dev;MODE=DB2;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.h2.Driver}

spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.H2Dialect}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}

spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:true}
