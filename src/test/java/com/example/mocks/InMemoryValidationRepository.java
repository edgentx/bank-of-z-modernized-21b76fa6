package com.example.mocks;

/**
 * Mock Repository for Validation context.
 * Currently serves as a placeholder to satisfy the repository pattern requirement
 * if the aggregate requires persistence lookups.
 */
public class InMemoryValidationRepository {
    // In-memory implementation stub
}